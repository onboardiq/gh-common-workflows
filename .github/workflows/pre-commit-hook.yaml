name: pre-commit-hook
on:
  pull_request:
    branches:
      - main
jobs:
  hook:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: brew install pre-commit && pre-commit run --all-files
